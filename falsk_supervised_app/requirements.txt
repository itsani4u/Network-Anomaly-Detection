flask==2.3.2                 # Flask for creating the API
numpy==1.26.0                # NumPy for numerical operations
pandas==1.5.3                # Pandas for data manipulation (if needed)
scikit-learn==1.3.1          # Scikit-learn for machine learning model
joblib==1.3.2                # Joblib for saving and loading the model
gunicorn==21.2.0             # Gunicorn for production deployment (if deploying to a server)
